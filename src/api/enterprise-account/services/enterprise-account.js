'use strict';

/**
 * enterprise-account service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::enterprise-account.enterprise-account');
